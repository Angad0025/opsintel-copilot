cat > ~/Desktop/opsintel-copilot/docs/demo_script.md << 'EOF'
# Demo Script

Use this script when showing OpsIntel Copilot in an interview or demo.

## Setup (Before the Demo)

Make sure both services are running:
- Streamlit: http://3.91.170.60:8501
- FastAPI: http://44.221.58.68:8000/docs

## Demo Flow (5 minutes)

### Step 1 — Open the Dashboard (30 seconds)

Open http://3.91.170.60:8501

Say: "This is OpsIntel Copilot — an AWS-native AI investigation platform I built. It's fully deployed on AWS ECS Fargate. The frontend is Streamlit, the backend is FastAPI, and all the data comes from RDS PostgreSQL which is populated by a Databricks pipeline."

### Step 2 — Show the Overview Page (45 seconds)

Click Overview in the sidebar.

Say: "The system is currently tracking 55 security alerts and 1,496 cross-domain correlations. These are all real records generated by my synthetic data pipeline and processed through Databricks Delta Lake."

### Step 3 — Show Security Intelligence (45 seconds)

Click Security Intelligence.

Say: "The security detection engine identified 50 brute force attacks, 2 impossible travel events, 1 privilege escalation, and 1 large data export. These were detected by PySpark rules running in Databricks."

Scroll down to show the filterable table.

Say: "You can filter by event type to investigate specific threat categories."

### Step 4 — Show Correlation Engine (60 seconds)

Click Correlation Engine.

Say: "This is the strongest feature. My PySpark correlation engine links data pipeline failures with security events using time-window rules. For example: if a suspicious admin config change happens within 30 minutes of a pipeline failure, the engine creates a security-to-pipeline correlation with a confidence score of 0.9."

Point to the table showing correlation_id, pipeline_name, error_message, security_event_type, involved_user, time_diff_minutes, confidence_score.

Say: "Every one of these 1,000 records was generated automatically by the correlation engine and stored in RDS PostgreSQL."

### Step 5 — Show the Investigation Copilot (90 seconds)

Click Investigation Copilot.

Type: "Which pipelines failed after a config change by security-admin and what should I do?"

Wait for the answer (10-15 seconds).

Say: "This is powered by Amazon Bedrock Knowledge Bases. The system searched 200 correlation records, 55 security alerts, and security playbooks stored in S3 — then used Claude Sonnet 4.6 to generate a grounded answer. Notice it cites the specific correlation IDs, timestamps, and source documents. It's not hallucinating — every fact comes from real data in my pipeline."

Point to the Sources section showing pipeline_failure_playbook.txt and correlation_summaries.txt.

### Step 6 — Show the GitHub Repo (30 seconds)

Open https://github.com/Angad0025/opsintel-copilot

Say: "Everything is on GitHub. The repo has dbt models with custom macros and tests, Terraform for all AWS infrastructure, GitHub Actions CI/CD running dbt test and SQLFluff on every PR, data contracts in YAML, and Databricks notebooks for the full pipeline."

## Interview Questions This Demo Answers

- "Tell me about a project you're proud of" → walk through the full demo
- "Have you used dbt?" → show dbt/models/ folder
- "What's a data contract?" → show contracts/ folder
- "Have you deployed to AWS?" → show ECS Fargate running live
- "What is RAG?" → show the copilot answer with sources
- "Have you worked with Databricks?" → show databricks/notebooks/
- "Tell me about your CI/CD experience" → show .github/workflows/
EOF